const express = require('express'); const cors = require('cors'); const app = express(); const PORT = 3000; app.use(cors()); app.use(express.json()); app.get('/api', (req, res) => { res.json({ message: 'Welcome to MEAN Stack' }); }); app.listen(PORT, () => { console.log(`Server is running on port ${PORT}`); });
